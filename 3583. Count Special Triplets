3583. Count Special Triplets


You are given an integer array nums.
A special triplet is defined as a triplet of indices (i, j, k) such that:
0 <= i < j < k < n, where n = nums.length
nums[i] == nums[j] * 2
nums[k] == nums[j] * 2
Return the total number of special triplets in the array.

Since the answer may be large, return it modulo 109 + 7.


Example 1:
Input: nums = [6,3,6]
Output: 1
Explanation:
The only special triplet is (i, j, k) = (0, 1, 2), where:
nums[0] = 6, nums[1] = 3, nums[2] = 6
nums[0] = nums[1] * 2 = 3 * 2 = 6
nums[2] = nums[1] * 2 = 3 * 2 = 6


Example 2:
Input: nums = [0,1,0,0]
Output: 1
Explanation:
The only special triplet is (i, j, k) = (0, 2, 3), where:
nums[0] = 0, nums[2] = 0, nums[3] = 0
nums[0] = nums[2] * 2 = 0 * 2 = 0
nums[3] = nums[2] * 2 = 0 * 2 = 0

Example 3:
Input: nums = [8,4,2,8,4]
Output: 2
Explanation:
There are exactly two special triplets:
(i, j, k) = (0, 1, 3)
nums[0] = 8, nums[1] = 4, nums[3] = 8
nums[0] = nums[1] * 2 = 4 * 2 = 8
nums[3] = nums[1] * 2 = 4 * 2 = 8
(i, j, k) = (1, 2, 4)
nums[1] = 4, nums[2] = 2, nums[4] = 4
nums[1] = nums[2] * 2 = 2 * 2 = 4
nums[4] = nums[2] * 2 = 2 * 2 = 4
 

Constraints:
3 <= n == nums.length <= 10^5
0 <= nums[i] <= 10^5


CODE:

class Solution {
public:
    int specialTriplets(vector<int>& nums) {
        int n = nums.size();
        int count = 0;

        if (n == 3) {
            if (nums[0] == nums[2] && 2 * nums[1] == nums[0])
                return 1;
            else
               return 0;
        }

        map<int, int> leftmp;
        map<int, int> rightmp;

        leftmp[nums[0]]++;

        for (int i = 2; i < n; i++)
            rightmp[nums[i]]++;

        for (int i = 1; i <= 1; i++) {
            if ((leftmp.find(2 * nums[i]) != leftmp.end()) &&
                (rightmp.find(2 * nums[i]) != rightmp.end())) {
                int n = leftmp[2 * nums[i]];
         int m = rightmp[2 * nums[i]];

                count += (n * m);
            }
            leftmp[nums[i]]++;
…
               }

        for (int i = 2; i < n; i++) {
            if (rightmp.find(nums[i]) != rightmp.end())
                rightmp[nums[i]]--;

            if ((leftmp.find(2 * nums[i]) != leftmp.end()) &&
                (rightmp.find(2 * nums[i]) != rightmp.end())) {
                int n = leftmp[2 * nums[i]];
                int m = rightmp[2 * nums[i]];

                count = (count + 1LL * n * m) % 10 ^ 9;
            }

            leftmp[nums[i]]++;

    return count;

        // for(int i=0; i<n; i++)
        // {
        //     for(int j=i+1; j<n; j++)
        //     {
        //         for(int k=j+1; k<n; k++)
        //         {
        //             if(nums[i]==nums[j]*2 && nums[k]==nums[j]*2)
        //              count++;
        //         }
        //     }
        // }
        // return count;

    }
};


THE COMMENTED PART IS THE BRTUTE FORCE APPROACH FOR THIS PROBLEM
